# Copy this file to ".env.local" (same folder) and fill it in.
# Use the same email + password you use to sign in to the Timeline app.
# ".env.local" is git-ignored and must never be committed or shared.
TIMELINE_EMAIL=you@example.com
TIMELINE_PASSWORD=
